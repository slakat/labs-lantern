<%= form_for @flashlight_gallery, :html =>{:multipart => true} do |f| %>
  <% if @flashlight_gallery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flashlight_gallery.errors.count, "error") %> prohibited this flashlight_gallery from being saved:</h2>

      <ul>
      <% @flashlight_gallery.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :image%>
  </div>
    <div class="field">
    <%= f.label :remote_image_url , "Or add a url"%><br/>
    <%= f.text_field :remote_image_url%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
