var $title=$("#modalTitle");
var $body=$("#modalContent");



$title.empty();
$body.empty();

$title.append('<%= escape_javascript @accessory.name %>');
$body.append('<%= escape_javascript render partial: "accessory", object: @accessory %>');

