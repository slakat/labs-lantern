var $title=$("#modalTitle");
var $body=$("#modalContent");



$title.empty();
$body.empty();

$title.append('<%= escape_javascript @accessory.name %>');
$body.append('<%= escape_javascript render @accessory.id , :locals=>{:accessory=>@accessory}%>');